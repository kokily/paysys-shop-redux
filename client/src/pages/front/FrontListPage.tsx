import React from 'react';
import PageTemplate from '../../components/common/PageTemplate';
import FrontListContainer from '../../containers/front/FrontListContainer';

const FrontListPage = () => {
  return (
    <PageTemplate>
      <FrontListContainer />
    </PageTemplate>
  );
};

export default FrontListPage;
